sudo: required
language: generic
services: 
  - docker
  
before_install:
  - docker build -t nikidenker/docker-react -f Dockerfile.dev .

script:
- docker run -e CI=true nikindenker/docker-react npm run test
